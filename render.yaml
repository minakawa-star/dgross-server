services:
  - type: web
    name: digross-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app_combined:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
